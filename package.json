{
  "name": "confetti",
  "description": "graphql api for music party",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --transpile-only --no-notify --exit-child ./src/index.ts",
    "generate": "ts-node --transpile-only ./src/schema.ts"
  },
  "dependencies": {
    "apollo-server": "^3.6.3",
    "graphql": "^16.3.0",
    "nexus": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.6.2"
  },
  "packageManager": "yarn@3.2.0"
}
